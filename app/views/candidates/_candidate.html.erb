<tr>
  <td><%= link_to "投給他/她/它", vote_candidate_path(candidate), method: "post", data: {confirm: "您確定?"}, class: "btn btn-danger btn-xs"%></td>
  <td><%= candidate.name %>(年齡: <%= candidate.age%> 歲)</td>
  <td><%= candidate.party %></td>
  <td><%= candidate.politics %></td>
  <td><%= candidate.vote_logs.size %></td>
  <td>
    <%= link_to "Edit", edit_candidate_path(candidate) %>
    <%= link_to "刪惹", candidate_path(candidate), method: "delete", data: { confirm: "確定???"} %>
  </td>
</tr>
